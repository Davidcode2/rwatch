apiVersion: v1
kind: Service
metadata:
  name: rwatch-agent
  namespace: rwatch
  labels:
    app: rwatch-agent
spec:
  selector:
    app: rwatch-agent
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  type: ClusterIP
  clusterIP: None
